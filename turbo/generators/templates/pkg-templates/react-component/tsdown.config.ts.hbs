import { defineConfig } from 'tsdown'

export default defineConfig({
  entry: ['./src/index.ts'],
  minify: true,
  target: 'es2020',
  clean: true,
  unbundle: true,
  external: ['react', 'react-dom']
})
